#include "main.h"

/**
 * add - FUunction returns the addition of two integers
 * @a: first integer to be added to -
 * @b: second integer
 *
 * Return: Integer
 */
int add(int a, int b)
{
	int result = (a + b);

	return (result);
}

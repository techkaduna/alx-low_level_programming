#include "main.h"
#include <stdlib>

/**
 * _abs - computes the absolute value of an integer
 * @n: integer whose absolute value is computed
 *
 * Return: abs(n)
 */
int _abs(int n)
{
	return (abs(n));
}

#include <stdio.h>

/**
 * main - print a et of numbers between 0-9
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int a;

	/*looping through an array of integers*/
	for (a = 0; a < 10; a++)
	printf("%d", a);
	putchar('\n');
	return (0);
}
